apiVersion: v1
kind: Namespace
metadata:
  name: jokejolt-staging
  labels:
    environment: staging
    app: jokejolt

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jokejolt
  namespace: jokejolt-staging
  labels:
    app: jokejolt
    environment: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jokejolt
  template:
    metadata:
      labels:
        app: jokejolt
        environment: staging
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: jokejolt
          image: ghcr.io/route/joke-jolt:develop-latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3000"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: jokejolt
  namespace: jokejolt-staging
  labels:
    app: jokejolt
    environment: staging
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: jokejolt
    environment: staging

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: jokejolt
  namespace: jokejolt-staging
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: jokejolt
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
